# RUN-PROFILE-01 — matmul nsys (4× H100) submission record

Tracked manifest. The raw `.nsys-rep`, `.cuda_gpu_sum.txt`, `.nvtxsum.txt`,
and stdout/stderr files land under
`experiments/results/2026-05-11_h100x4_matmul-mgpu-nsys/raw/` once the job
COMPLETES; that directory is git-ignored.

| Field | Value |
|---|---|
| Slice | `RUN-PROFILE-01` |
| SLURM script | `scripts/mn5/slurm_matmul_mgpu_nsys.sh` |
| Binary | `build/bin/matmul_align_n8k` |
| Invocation | `--n-gpus 4 --trials 3` (median of 3 trials, under nsys) |
| Host | MareNostrum 5 ACC partition (`acc` / `acc_ehpc`) |
| Submitted | 2026-05-11 evening (CEST) |
| JOBID | **40417847** |
| Initial state | PENDING (Reason: Priority) |
| Walltime budget | 00:20:00 |

## Build provenance

`build/bin/matmul_align_n8k` rebuilt on MN5 immediately before submission
against the post-FIX-BUG-04-04 source tree:

- `src/nexus_eval/matrix_mul.cu` carries the persistent pinned host-staging
  buffer hoisted out of `multiply_power_of_x` (commit `80dc737`).
- `src/benchmarks/matmul_align_n8k.cu` carries the FIX-BUG-01-01 single-GPU
  absolute MAE gate (`mae_single < 5e-2`) at Phase 1, on top of the
  pre-existing Phase 2 5%-relative gate (commit `b5e7457`).

Build was incremental (`make -j20 matmul_align_n8k`) on `cuda/12.8 +
cmake/3.30.5 + nccl/2.24.3-1`. Static lib `libnexus_evaluators.a` and the
final executable both relinked.

## Expected output paths (on MN5)

| File | Path |
|---|---|
| stdout | `/gpfs/projects/etur02/hkanpak/logs/matmul_mgpu_nsys_40417847.out` |
| stderr | `/gpfs/projects/etur02/hkanpak/logs/matmul_mgpu_nsys_40417847.err` |
| nsys-rep | `/gpfs/projects/etur02/hkanpak/logs/matmul_mgpu_nsys_40417847.nsys-rep` |
| cuda_gpu_sum | `/gpfs/projects/etur02/hkanpak/logs/matmul_mgpu_nsys_40417847.cuda_gpu_sum.txt` |
| nvtxsum | `/gpfs/projects/etur02/hkanpak/logs/matmul_mgpu_nsys_40417847.nvtxsum.txt` |

## What §6 of the paper needs from this run

`paper/sections/06_per_op_typology.md` §6.4 has a `[TODO: confirm with
PROFILE-01 trace]` marker against the MatMul row's parallelization-grounded
ceiling explanation. The post-run BACKFILL-S6 slice will:

1. `scp` the four result files into `raw/` of this directory.
2. Read `cuda_gpu_sum.txt` for top-3 kernel time share + NCCL share.
3. Read `nvtxsum.txt` for `MATMUL_OUTPUT_CHANNEL_SPLIT` per-rank time.
4. Replace the `[TODO]` with the measured share/explanation.
5. Re-run `scripts/regression/result_aggregator.py` against this dir to
   refresh the §6.4 cross-bucket summary table cell for matmul.

## Status checks

```bash
# Job state
ssh mn5-gpu "squeue -j 40417847 -o '%i %T %r %S %L %D %P'"

# Once COMPLETED, fetch artifacts:
scp mn5-gpu:/gpfs/projects/etur02/hkanpak/logs/matmul_mgpu_nsys_40417847.* \
    experiments/results/2026-05-11_h100x4_matmul-mgpu-nsys/raw/
```
