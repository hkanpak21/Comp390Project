# RUN-MEASURE-03 — HP-BERT DP throughput (4-GPU) submission record

Tracked manifest. Aggregate output and per-GPU logs land at
`/gpfs/projects/etur02/hkanpak/logs/bert_hp_throughput_4gpu_40418704.*`.

| Field | Value |
|---|---|
| Slice | `RUN-MEASURE-03` |
| SLURM script | `scripts/mn5/slurm_bert_hp_throughput_4gpu.sh` |
| Binary | `build/bin/bert_hp_multigpu` |
| Invocation | G=4 concurrent `--N 32768 --n-gpus 1 --heads 12 --layers 12 --skip-ref` |
| Host | MareNostrum 5 ACC partition (`acc` / `acc_ehpc`) |
| Submitted | 2026-05-11 evening (CEST) |
| JOBID | **40418704** |
| Initial state | PENDING (Reason: None) |
| Walltime budget | 01:00:00 |

## Build provenance

`build/bin/bert_hp_multigpu` rebuilt on MN5 immediately before submission
(same as RUN-MEASURE-01: post-FIX-BUG-04-02 and post-FIX-BUG-02-01 sources).

Build was incremental (`make -j20 bert_hp_multigpu`) on `cuda/12.8 +
cmake/3.30.5 + nccl/2.24.3-1` and clean.

## Expected output paths (on MN5)

| File | Path |
|---|---|
| stdout | `/gpfs/projects/etur02/hkanpak/logs/bert_hp_throughput_4gpu_40418704.out` |
| stderr | `/gpfs/projects/etur02/hkanpak/logs/bert_hp_throughput_4gpu_40418704.err` |
| gpu0 log | `/gpfs/projects/etur02/hkanpak/logs/bert_hp_throughput_4gpu_40418704_gpu0.log` |
| gpu1 log | `/gpfs/projects/etur02/hkanpak/logs/bert_hp_throughput_4gpu_40418704_gpu1.log` |
| gpu2 log | `/gpfs/projects/etur02/hkanpak/logs/bert_hp_throughput_4gpu_40418704_gpu2.log` |
| gpu3 log | `/gpfs/projects/etur02/hkanpak/logs/bert_hp_throughput_4gpu_40418704_gpu3.log` |

## What §7 of the paper needs from this run

`paper/sections/07_end_to_end.md` weak-scaling section needs:

1. **Per-GPU wall time**: time(gpu_i) for each i in [0, 3].
2. **Aggregate wall time**: max(time_i) across 4 GPUs.
3. **Aggregate throughput (inferences/s)**: 4 / aggregate_wall_time.
4. **Per-instance exit status**: all four should be OK (MAE gates passed).

The post-run BACKFILL-S7 slice will:
1. Extract per-GPU wall times from the four gpu*.log files.
2. Compute aggregate wall time and throughput from main stdout.
3. Fill the 4-GPU row in the weak-scaling table in §7.

## Status checks

```bash
ssh mn5-gpu "squeue -j 40418704 -o '%i %T %r %S %L %D %P'"

# Once COMPLETED:
ssh mn5-gpu "grep -E 'Aggregate|throughput' /gpfs/projects/etur02/hkanpak/logs/bert_hp_throughput_4gpu_40418704.out"
ssh mn5-gpu "head -20 /gpfs/projects/etur02/hkanpak/logs/bert_hp_throughput_4gpu_40418704_gpu*.log"
```
